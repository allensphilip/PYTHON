#program to convert kilometres to miles. Take the input from the user.

distkm = float(input("Enter the distance in KiloMeters: "))
distmiles = distkm / 1.609
print("The converted distance in Miles: ", +distmiles)
